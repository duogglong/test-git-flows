apiVersion: apps/v1
kind: Deployment
metadata:
  name: one-man-work-v1
spec:
  selector:
    matchLabels:
      app: one-man-work-pod
  template:
    metadata:
      labels:
        app: one-man-work-pod
    spec:
      containers:
        - name: one-man-work-pod
          image: REGION_REGISTRY/GOOGLE_PROJECT/REGISTRY_PROJECT/develop/one-man-work:VERSION
          imagePullPolicy: Always
          ports:
            - containerPort: 9988
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "SPRING_PROFILES_ACTIVE_VARIABLE"
            - name: DATABASE_URL
              value: "DATABASE_URL_VARIABLE"
            - name: DATABASE_USERNAME
              value: "DATABASE_USERNAME_VARIABLE"
            - name: DATABASE_PASSWORD
              value: "DATABASE_PASSWORD_VARIABLE"